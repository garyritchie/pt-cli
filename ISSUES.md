# Issues and Feature Requests

## Issue


## Feature Request

- [ ] Global post_config. We need a mechanism that allows adding a global post_config setting, during learn, to the template. Maybe a top-level key in config.yaml can be used to store "global" options then, during "learn" those options appear in the post_config section with a chechbox that can be unchecked if the user does not want it added to that template. Reasoning: These days almost all of my projects use git and it can get old editing the config.yaml each time I want `git init ...` and other commands run on project init.
