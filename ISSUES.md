# Issues and Feature Requests

## Issue

## Feature Request

- [x] Allow for the export of any one template by name using `pt config templateName --json` or similar. Minimal editing of the project to produce this feature or provide a plan if more complicated.
- [ ] Make templateRoot optional for folders -- create folder structure only from config.yaml folders key instead of relying on copy operation from templateRoot. This would allow for more portable folder-only templates.